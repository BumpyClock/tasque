# Command Reference

Read when: you need exact command syntax or available options.

Canonical CLI and task contracts live in [`AGENTS-reference.md`](../../../AGENTS-reference.md).

Use the binary for the freshest syntax:

```bash
tsq --help
tsq <command> --help
```

This skill reference intentionally avoids a second hand-maintained command matrix.
